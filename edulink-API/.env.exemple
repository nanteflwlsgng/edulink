# JWT
JWT_SECRET=MaSuperCleSecretePourJWT

# SMTP / Email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=monemail@gmail.com
SMTP_PASS=MonAppPassword

# Optionnel : PDF
PDF_PATH=./pdfs
# PDF_PATH=/app/pdfs
